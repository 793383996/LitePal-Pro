name: Core PR Gate

on:
  pull_request:
    paths:
      - "core/**"
      - "compiler-common/**"
      - "compiler-ksp/**"
      - "compiler-kapt/**"
      - "fixture-runtime-stubs/**"
      - "fixture-ksp-consumer/**"
      - "fixture-kapt-consumer/**"
      - "fixture-negative/**"
      - ".github/workflows/core-pr-gate.yml"
      - "build.gradle"
      - "settings.gradle"
      - "gradle/**"
      - "gradle.properties"
  push:
    branches:
      - main
      - master
    paths:
      - "core/**"
      - "compiler-common/**"
      - "compiler-ksp/**"
      - "compiler-kapt/**"
      - "fixture-runtime-stubs/**"
      - "fixture-ksp-consumer/**"
      - "fixture-kapt-consumer/**"
      - "fixture-negative/**"
      - ".github/workflows/core-pr-gate.yml"
      - "build.gradle"
      - "settings.gradle"
      - "gradle/**"
      - "gradle.properties"

jobs:
  verify-core:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Verify Java and Gradle runtime
        run: |
          java -version
          ./gradlew -version

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Core compile, parity, unit tests
        run: |
          ./gradlew \
            :compiler-common:test \
            :core:assembleDebug \
            :core:testDebugUnitTest \
            verifyGeneratedOnlyRuntimeGuards \
            verifyKspKaptGeneratedParity \
            verifyProcessorNegativeCases \
            --no-daemon \
            --stacktrace

      - name: Core lint (isolated Gradle session to avoid metaspace accumulation)
        run: |
          ./gradlew \
            :core:lint \
            --no-daemon \
            --stacktrace

      - name: Core publish dry-run (isolated Gradle session to avoid metaspace accumulation)
        run: |
          ./gradlew \
            :core:publishReleasePublicationToMavenLocal \
            --no-daemon \
            --stacktrace
